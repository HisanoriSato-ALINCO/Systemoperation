<%= form_with(model: msystem) do |form| %>
  <% if msystem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(msystem.errors.count, "error") %> prohibited this msystem from being saved:</h2>

      <ul>
        <% msystem.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
